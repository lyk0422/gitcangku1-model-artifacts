# Model Round Artifacts

This repository stores trace JSON files separately from generated source code.

Each run publishes files below `model-round-artifacts/<run-id>/<task-id>/<side>/attempt-xx/` on its own `runs/<run-id>` branch.
